// * Custom Cypress commands for FantasyWritingApp
// * This file imports all modular command files to keep the structure organized

// * Import all command modules via the index file which properly organizes them
import './commands/index'; // This imports the index.ts which handles all the subdirectory imports

// * Export empty object to prevent TypeScript errors
export {};