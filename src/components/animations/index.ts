/**
 * Animation components index
 * * Export all animation-related components and hooks
 */

export { ContentReveal } from './ContentReveal';
export type { RevealAnimation } from './ContentReveal';

// * Export hooks from the hooks directory
export { useRevealAnimation, useStaggeredReveal } from '../../hooks/useRevealAnimation';
export type { AnimationType } from '../../hooks/useRevealAnimation';